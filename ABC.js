abc = function(a,b) {
  // ARGS: A<a> and B<b> components of LAB value for base color
  // RTNS: <c>, being a function that takes a value number in an  
  c = {}
  // Normal Set
  // Emphasis 'AXXX' set
  return c
}